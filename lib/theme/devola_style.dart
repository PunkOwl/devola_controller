
import 'package:flutter/material.dart';

ThemeData devolaTheme() {
  return ThemeData(
    primaryColor: Colors.black,
    canvasColor: const Color(0xffe4e6eb),
    scaffoldBackgroundColor: const Color(0xffe4e6eb),
    appBarTheme: AppBarTheme(
      color: Colors.black,
    )
  );
}